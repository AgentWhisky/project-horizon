{
  "name": "project-horizon",
  "version": "1.0.0",
  "description": "Web project",
  "scripts": {
    "client": "cd client && npm install && npm run build",
    "server": "cd server && npm install && npm run build",
    "heroku-prebuild": "npm run server && npm run client",
    "start": "cd server && npm run start:prod"
  },
  "dependencies": {
    "@angular/cli": "^19.0.2",
    "@nestjs/cli": "^10.0.0",
    "@types/multer": "^1.4.12"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "18.x"
  }
}
